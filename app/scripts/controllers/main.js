'use strict';

angular.module('returnBoostApp')
  .controller('MainCtrl', ['$scope', function ($scope){
/*    $timeout(function() {
      navigation.goStep(1);
    }, 2000)

    $timeout(function() {
      navigation.goStep(0);
    }, 4000)*/
  }]);
